[package]
name = "canary-scheduler"
description = "Canary Engine ECS scheduler: system access declarations and stage-based execution."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
canary-ecs = { path = "../canary-ecs" }

[dev-dependencies]
divan = { version = "5", package = "codspeed-divan-compat" }

[[bench]]
name = "scheduler"
harness = false
