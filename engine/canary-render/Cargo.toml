[package]
name = "canary-render"
description = "Canary Engine RHI trait: devices, buffers, pipelines, command encoding. Backend-agnostic -- see canary-render-vulkan and ADR 0016."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
