[package]
name = "canary-runtime"
description = "Canary Engine headless boot-harness binary: exercises core/platform/ecs/plugin-api together."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
canary-assets = { path = "../canary-assets" }
canary-core = { path = "../canary-core" }
canary-platform = { path = "../canary-platform" }
canary-ecs = { path = "../canary-ecs" }
canary-plugin-api = { path = "../canary-plugin-api" }
canary-physics = { path = "../canary-physics" }
canary-render-ecs = { path = "../canary-render-ecs" }
canary-scheduler = { path = "../canary-scheduler" }
canary-transform = { path = "../canary-transform" }
anyhow = "1.0.104"
tracing = { version = "0.1.44", default-features = false, features = ["std"] }
