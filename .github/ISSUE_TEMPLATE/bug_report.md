---
name: Bug report
about: Something in the engine, tooling, or build doesn't work as documented
title: "[bug] "
labels: bug
---

**Describe the bug**
A clear, concise description of what's wrong.

**Crate / subsystem**
e.g. `canary-ecs`, `canary-plugin-api`, `xtask`, build system, docs.

**To reproduce**
Minimal steps to reproduce. A minimal `cargo run`/`cargo test` repro is ideal.

```
# commands here
```

**Expected behavior**
What you expected to happen instead.

**Environment**
- Canary version / commit:
- OS:
- Rust toolchain (`rustc --version`):

**Additional context**
Logs, stack traces, or anything else that helps.
